/**
 * CORE CONTEXTUAL COMMENT — NormalizationTypes.ts
 * -----------------------------------------------
 * Enumerations for available normalization strategies.
 */
export enum NormalizationStrategies {
  NONE = "NONE",
  Z_SCORE = "Z_SCORE",
  MIN_MAX = "MIN_MAX",
}
